# SD diagnostics checkpoint — 2026-09-17, end of day

Resume update (2026-09-18 09:31): boot 60 passes both duration cases and USB
fixture deletion. Archive 18 was verified and deleted in 52.51 s; exactly 2 MiB
was recovered, four managed files remain, and logging has zero drops. Source
now defaults `DIAG_USB_TEST_FIXTURE=0`; the running board still has it enabled
until JP reflashes. Queue-pressure and selected-archive pruning checks remain
open, so Stage 1B is not fully accepted. JP can now flash the normal configuration
and check status plus one current.log download; further fault tests will use a
separate bench build.
See the [bench results](sd_diagnostics_bench_results.md) and
[test controls](../src/diagnostics/STAGE1B.md).

## Where we stopped

JP accepted Stage 1 (SD logging basics). Stage 1B (USB retrieval) is implemented
and substantially bench-tested, but its remaining gate is not yet accepted.
Stages 2–4 have not started. JP builds and flashes from VS Code.

The working branch is `sd-diagnostics`, using profile `amoled-1-8-core-3-3-11`.
Retain the tested one-second sustained USB connection-loss guard and five-second
no-progress guard. A false connection reading pauses sending immediately;
short glitches can recover. CRC and sequence validation remain mandatory.
Current downloads retain a 120-second overall limit; archives have none.

**Repository and board differ tonight:** the optional Live pacing experiment
has been reverted in source. JP's last flashed board (boot 55) still contains
that experiment. Tomorrow, build and flash the restored source before collecting
more results. All reverted edits were under `src/`; no generated
`companion.ino.cpp` deletion is required for this update. No build or flash
was performed by Codex. The experiment was never committed, so the source
reversion restores the firmware already in commit `a1dae68`.

## What passed

- Existing small archive matched the independent card backup byte for byte.
- Current snapshots and current-plus-recent-archive downloads completed, with
  logging resuming, no lost records reported, and CRC validation.
- Damaged-line rejection, five-second stopped-reader timeout, and subsequent
  successful retries were exercised. The earlier unexplained early stall is
  retained as unresolved history, not declared fixed by later successes.
- Synthetic archive 18 contains exactly **2097152 bytes**. Three unpaced
  downloads and one paced download matched every independently generated byte.
  Fixture CRC32 is `8D218D21`; reference generator is `tools/verify_usb_fixture.py`.
- Ordinary 2 MiB download: **20.39 s**. MQTT-overlap download: **22.67 s**, including
  initial command wait; completion occurred during a blocking MQTT retry.
  Real MQTT reconnection then took **618 ms**.
- The core connection indication briefly went false for **3–4 ms** during
  successful transfers. The new guard tolerated these observations.
- Large Live-overlap integrity and memory checks passed. Largest internal block
  remained **28660 bytes**, above the unchanged **20480-byte** memory floor;
  writer margin after downloads was **3320 bytes**, with no logger errors/drops.

## Live performance decision

| Configuration | Live-only FPS | Live with 2 MiB download | Download time |
|---|---:|---:|---:|
| Normal USB pacing, boot 53 | 3.021 before / 3.002 after | 2.766 | 18.66 s |
| Experimental Live pacing, boot 55 | 2.992 | 2.833 | 54.73 s |

The original concurrent run was about 8% slower than its controls. The optional
pacing experiment still missed the 5% target (5.29% lower) while nearly tripling
file-transfer time. Overlap duration also changed, so these are not an isolated
measurement of per-second CPU overhead. JP saw normal video in every run.

JP agreed to revert pacing and accept the uncommon large-download-during-Live
cost rather than add complexity. Record this as a **narrow exception to the
5% Live FPS criterion for simultaneous large USB downloads**, not a passed
5% comparison or a general relaxation of performance/memory requirements.
No further pacing optimization is planned. Normal 2 MiB and MQTT-overlap
measurements meet the 120-second deadline's 3x margin. The slower experiment's
164.19-second 3x requirement is not retained with the reverted pacing policy.

## Remaining Stage 1B work

Resume one test at a time. Preserve passed results rather than repeating the
whole suite. After JP flashes the restored source, take status and one normal
download to confirm the bench is on the intended configuration.

1. Explicit abort/retry, page-close/retry and battery-powered physical
   USB-unplug/retry passed on 2026-09-18. No repeat currently needed.
   JP's board has a battery: unplug USB only with the battery powering it.
   Account for the existing inactivity shutdown timer; check subsequent logging.
2. Both duration cases passed on 2026-09-18: current timeout and recovery at
   09:19; a progressing archive completed in 154.83 s at 09:25 with exact bytes.
3. Exercise the **50% queue** guard while current appends are paused, and pruning
   of the archive being read. Check cleanup, continued logging and zero drops.
   Plan any required test-hook build separately; fixture and fault-hook flags
   must not be enabled together.
4. Review remaining real-current snapshot/prefix integrity evidence, accumulated
   memory/handle stability, and the Web Serial/VS Code round-trip limitation.
   Repeated success/abort/retry evidence already exists; repeat only uncovered cases.
5. Fixture deletion over USB passed at 09:30 on 2026-09-18. Archive 18 is gone,
   four managed files remain, and exactly 2 MiB was recovered. No repeat needed.
6. Source defaults now have `DIAG_USB_TEST_FIXTURE=0`, `DIAG_TEST_HOOKS=0` and
   PSRAM writer enabled. JP's boot 60 still contains the USB test commands.
   After remaining checks and final ordinary-build verification, JP accepts
   Stage 1B with explicit limitations before Stage 2 starts.

Keep test source for future use, excluded from normal builds. Prepare a separate
sacrificial file if needed for the remaining pruning test; preserve real archives.
No card removal was needed for fixture cleanup.

## Main project steps afterward

- **Stage 2 — network evidence:** SD records for Wi-Fi events and profiles,
  every MQTT attempt and result, failure details before cleanup, and TLS markers.
  Test with hotspot interruptions and the existing `off` / `on` commands.
- **Stage 3 — operation context:** still/Live lifecycle, buttons/screens,
  power/motion transitions and explained loop gaps. Optional log tail comes last.
- **Stage 4 — car validation:** use real trips and downloaded incident logs;
  tune retention from measured growth and confirm field operation.

Core 3.3.11 remains a trial, although basic board functions and USB retrieval
have passed. Closing the VS Code monitor can still freeze/reset the board.
New-core normal IMU rate is about 42–43 Hz versus historical ~49 Hz; the old
figures are not a same-session comparison. Keep this migration follow-up visible.
No claim that every USB/core issue is solved or that Stage 1B is fully accepted.

Detailed evidence: [bench results](sd_diagnostics_bench_results.md),
[core trial](core_3_3_11_trial.md), [USB handoff](../src/diagnostics/STAGE1B.md),
[design and remaining gates](sd_diagnostics_plan.md).
